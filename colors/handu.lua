local theme_template = require "themetemplate"

local theme_colors = {
	guicolors = true,
	type = "light",
	background = "#FEEADF",
	foreground = "#39261F",
	accent = "#B44C33",
}

theme_template.apply(theme_colors)
